<?php

/**
 * @file 
 * Contains tests for the Drupal Starterkit module.
 */

/**
 * Test case.
 */
class DrupalStarterkitTestCase extends DrupalWebTestCase {

  /**
   * Implements getInfo().
   */
  public static function getInfo() {
    return array(
      'name' => t('Drupal Starterkit tests'),
      'description' => t('TODO: write me.'),
      'group' => t('Drupal Starterkit'),
    );
  }

  /**
   * Implements setUp().
   */
  function setUp() {
    // Call the parent with an array of modules to enable for the test.
    parent::setUp(array('drupal_starterkit'));

    // TODO: perform additional setup tasks here if required.
  }

  /**
   * Test the module's functionality.
   */
  function testTodoChangeThisName() {
    // TODO: write test!
  }

}
